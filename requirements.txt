# Embedding / Vektor-DB
sentence-transformers>=3.3.0
lancedb>=0.15.0
torch>=2.4.0
numpy>=1.26.0  # u. a. für Auto-Sammlungen (Clustering)

# Dokument-Extraktion
pypdf>=5.1.0

# Web-Extraktion / Link-Prüfung
# trafilatura (GPLv3) liefert die beste Hauptinhalts-Erkennung; bs4 als Fallback.
httpx>=0.27.0
trafilatura>=1.12.0
beautifulsoup4>=4.12.0
lxml>=5.3.0

# Logging
structlog>=24.4.0

# MCP-Server (für server/server.py)
mcp>=1.2.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
