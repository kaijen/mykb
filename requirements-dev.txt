# Test-/CI-Abhängigkeiten.
#
# BEWUSST OHNE torch / sentence-transformers / mcp: Diese sind in der CI nicht
# installierbar bzw. nicht nötig. Tests dürfen daher weder einen echten
# Embedder instanziieren noch den MCP-Server importieren. Für LanceDB werden
# Dummy-Vektoren kleiner Dimension verwendet.

# Test-Runner und Linter
pytest>=8.0.0
ruff>=0.6.0

# Laufzeit-Deps, die für die getesteten Module nötig sind
lancedb>=0.15.0
httpx>=0.27.0
beautifulsoup4>=4.12.0
lxml>=5.3.0
trafilatura>=1.12.0
structlog>=24.4.0
fastapi>=0.115.0
python-multipart>=0.0.9
numpy>=1.26.0
